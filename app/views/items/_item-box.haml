.item-box
  = link_to item_path(item) do
    .item-box__photo
      = render "items/sold-tag", item: item
      = image_tag(item.item_photos.first.photo.to_s)
    .item-box__body
      %h3.item-box__body--name
        = item.name
      .item-box__body--info.clearfix
        .item-price
          = "¥" + item.price.to_s(:delimited)
        - if item.likes.length > 0
          .item-like
            = link_to "" , class: "" do
              = fa_icon "heart-o"
              = item.likes.length
        %p.item-tax
          (税込)
